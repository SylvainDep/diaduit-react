import layout from '../styled/layout/layout.styled';
import navigation from '../styled/navigation/navigation.styled';

const style_mapping = {
    layout,
    navigation
};

export { style_mapping };
